
<li class="dropdown">
    <a asp-controller="offee"
       asp-action="Index"
       class="dropdown-toggle" data-toggle="dropdown">Coffees<b class="caret"></b></a>
    <ul class="dropdown-menu">
        @foreach (var category in Model)
        {
            <li>
                <a asp-controller="Coffee" asp-action="List"
                   asp-route-category="@category.CategoryName">@category.CategoryName</a>
            </li>
        }
        <li class="divider"></li>
        <li>
            <a asp-controller="Coffee" asp-action="List" asp-route-category="">View all coffees</a>
        </li>
    </ul>
</li>
